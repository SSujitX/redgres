# Requirements traceability matrix

This file prevents “documented” from being mistaken for “implemented.” The table is current status. Empty evidence means incomplete. Do not mark a row Complete from prose alone.

Historical slice ledgers: `git show ee659b7:docs/TRACEABILITY.md` (through SQLite restore-verification) and `git log -p -- docs/TRACEABILITY.md` (later current-slice blocks). Do not preload that history.

New work replaces **one** current-slice block below (requirement, files, commands actually run, limitations). Do not re-copy older slices.

| Requirement group | Design source | Planned implementation | Test evidence | Status |
|---|---|---|---|---|
| AUTH-001..006 | PRD, Security, ADR-005 | `internal/auth`, `internal/httpapi` | AUTH-001–005 unit/HTTP/CLI tests; AUTH-005 Partial: fail-closed attempt persistence, loopback-only `CF-Connecting-IP` identity, reserve-before-Argon2, IP-wide spray only for non-loopback IPs (live 429 + Retry-After lockout over the real attempt store in `integration`); AUTH-006 Partial: in-handler `Reauthenticate` on `DELETE /api/v1/redis/users/{username}`, flagged `DELETE /api/v1/postgres/databases/{db}/tables/{schema}/{table}/rows`, flagged `POST /api/v1/postgres/databases/{db}/truncate`, and flagged `DELETE /api/v1/postgres/databases/{db}` (no `POST /api/v1/auth/reauth`; reauth failures persist in `login_attempts` under a reserved `client_ip` prefix, not the login stream) | Partial |
| PLAT-001..004 | PRD, Architecture, UX, UI Design System | `internal/platform`, `internal/audit`, `web/` | `GET /api/v1/healthz`; authenticated `GET /api/v1/status` + Overview live cards (PLAT-001 Partial: Redis Ping + Overview metrics, PgBouncer `SHOW VERSION` Ping (live console probe on 17.11/18.6 in `integration` via the §8 `edoburu/pgbouncer` pin), optional tool-link session hostnames + status presence, compact Overview `GET /api/v1/audit?limit=8` Recent audit When/Action/Target/Outcome, shell-owned responsive aggregate health, allow-listed `/assets/` full wordmark with compact favicon-only mark, and PostgreSQL quick-create routed to the existing dialog; no release line or live matrix); System page Partial: same five GET `/status` cards, Expert tools Open + pgAdmin reveal/copy (ADR-014), no Redis metrics, no raw tool hrefs, no host/DSN/version; PLAT-003 audit read API + history UI + Overview compact first page (live cursor pagination in `integration`); PLAT-004 `GET /api/v1/search` + grouped palette (live service-grouped hits for PG databases and Redis ACL users) (Partial: Redis ACL username hits; curated in-app Documentation catalog + client `filterDocs` article hits; no deep links/command palette) | Partial |
| PG-001..012 | PRD, Source Systems, ADR-004, ADR-010/011 | `internal/postgresadmin`, `internal/secrets`, `internal/operations`, `internal/backup` | PG-001/002 unit+HTTP+UI; PG-007 table-list API+UI + row-browse API+UI; PG-008 Partial: GET `/api/v1/postgres/databases/{db}/tables/{schema}/{table}/primary-key` + flagged `DELETE …/rows` API (`postgres.destructive` + CSRF + AUTH-006) + inspector single-column PK checkboxes and danger Delete selected dialog (live service-level lifecycle on 17.11/18.6 in `integration`; no HTTP/UI live, no Playwright); PG-012 Partial: GET `/api/v1/postgres/security` cluster overview + Security overview page + vault existence (`missing_password_count` when ok) + `rotation_eligible` (diagnostic; POST rotate is PG-006; live `rotation_eligible=true` for a vaulted LOGIN owner in `integration`); PG-005 Partial: in-process Fernet/KDF fixtures plus HTTP vault existence GET plus masked connection GET plus POST `/connection/reveal` (no Gate 4); PG-004 Partial: GET `/api/v1/postgres/databases/{db}/connection` masked URLs (no decrypt); PG-003 Partial: POST `/api/v1/postgres/databases` (`postgres.provision` + CSRF) + `secrets.Encrypt` + vault INSERT + compensation + Databases Create dialog + ticket-open nav/search guard + list GET 401 clears ticket (live service+HTTP matrix in `integration` on 17.11/18.6, incl. live failure compensation on a broken vault; no Gate 4); PG-006 Partial: POST `/api/v1/postgres/databases/{db}/credentials/rotate` (`postgres.credentials` + CSRF) + ALTER ROLE + vault upsert + inspector Rotate (live rotate + AUTH-006 reauth in the HTTP matrix; no Gate 4); PG-010 Partial: POST `/api/v1/postgres/databases/{db}/duplicate` (`postgres.provision` + CSRF) **202 after InsertQueued** (no credential, never waits for TEMPLATE; live 202→worker→succeeded journey on 17.11/18.6 in `integration`) + worker `RunQueuedDuplicates` (15m) TEMPLATE clone + unique owner + vault INSERT + clone-only compensation + inspector Duplicate **202** polls GET `/api/v1/operations/{id}` every 1s, no ticket, no auto-Reveal (live in-flight duplicate → 409 `operation_in_progress` in `integration`); nav `postgres-create` during that poll is consumed and does not open Create after the poll finishes; `cmd/redgres` Open → Reconcile(live Probe, Compensator) → 1s poller (live binary serve + poller journey on 17.11/18.6 in `integration`); no AUTH-006; clone transfer has no blanket grants/default-privilege expansion, preserves skipped-object ACLs, and uses transaction-scoped temporary membership; direct `deptype='e'` members are skipped, while subsidiary/internal extension behavior remains unproven (no live PG 17/18). ADR-010 freeze: `002_operations.sql` + `operations.Store` (`4f9a14a`) + GET `/api/v1/operations/{id}` (`platform.read`); duplicate POST is **202 after InsertQueued**; worker + `cmd/redgres` 1s poller wired. PG-009 Partial: flagged `POST /api/v1/postgres/databases/{db}/truncate` (`postgres.destructive` + CSRF + AUTH-006, one `TRUNCATE … RESTART IDENTITY`) + inspector danger Truncate dialog (no live PG, no Playwright); PG-011 Partial: flagged `DELETE /api/v1/postgres/databases/{db}` (`postgres.destructive` + CSRF + AUTH-006, terminate excluding current backend then `DROP DATABASE`, optional `DROP ROLE` + vault DELETE; HTTP backup gate on `260f1ad`: `backup.LoadCurrent` jail-root `current.json` + `EvaluateDropGate` after AUTH-006 and before DROP; unset catalog `503` **Backup catalog is not configured.**; deny `403` with evaluator reason; live real-artifact dump→isolated-restore→DROP gate journey on 17.11/18.6) + inspector danger Drop dialog (no live PG, no Playwright) | Partial |
| PG-011 artifact-integrity refinement | Architecture, API, Security, Backup, ADR-011 | `internal/postgresadmin.DropAfterValidation`, `internal/backup.VerifyPostgresDatabaseArtifact`, `internal/httpapi` | Target lock + manageable pre-read before backup I/O; manifest evaluation; real jailed artifact identity/size/SHA-256 verification; final policy re-read before SQL; protected/missing targets stay collapsed 404; verification failure is secret-safe 503 with zero terminate/drop calls; live tamper regression exists but was not rerun locally without Docker | Partial |
| REDIS-001..008 | PRD, Source Systems, ADR-006 | `internal/redisadmin` | REDIS-001 Partial: Ping on GET `/api/v1/status`; configured Open probes `INFO server` (not Ping) and fail-closes via `internal/release`; metrics + typed failures on GET `/api/v1/redis/status` + Overview (live metrics on 8.8.2/8.2.9 in `integration`); REDIS-002 Partial: ACL list/inspect GET + UI; REDIS-003/004 Partial: POST create `on` + named presets + GET `/api/v1/redis/presets` + Permission presets catalog page (one Queue/worker `h2` + kind `h3`s) + one-time ticket (live representative workloads for Lists/Streams/Sorted Sets on 8.8.2/8.2.9 in `integration`); REDIS-005 Partial: custom PATCH + POST create custom through `AllowedCommands()` + GET `/api/v1/redis/commands` + Edit/Create Custom checklists (no categories); REDIS-006 Partial: PATCH named-preset prefix/grants (password preserved) + inspector Edit permissions; REDIS-007 Partial: POST enable/disable `on`/`off` plus rotate `resetpass` + `>password` and inspector UI; REDIS-008 Partial: `DELETE /api/v1/redis/users/{username}` (`ACL LIST` + one `ACL DELUSER`) + inspector Delete danger dialog (live behavior matrix on 8.8.2/8.2.9: create/inspect/isolation/patch-password-preserved/rotate-invalidates/enable-disable/delete + queue Streams workload + protected/allow-list guards; real-Redis ACL parse fidelity fixed; no TLS, no Playwright, no CLIENT KILL, keys not deleted) | Partial |
| OPS-001..007 | Deployment, Installer, PostgreSQL Provisioning, Backup, Compatibility, ADR-005/008/009/011 | `deploy/`, `internal/platform`, `internal/backup`, `internal/database` | OPS-001/006 Partial: fail-closed dispatcher + `--dry-run` stage print; privileged direct-exec bootstrap fixes runtime PATH/locale/umask, validates and descriptor-pins the source tree, and applies an absolute no-symlink/no-writable-ancestry trust policy to operator files; main-path lifecycle config parser accepts only five documented keys from one ≤64 KiB pinned snapshot and treats values as inert data (syntax only; values not applied); Go `internal/release` allow-list (PG 17|18, Redis 8.2|8.8, policy revision `1`) + `REDGRES_REDIS_EXPECTED_SERIES` Load/Open identity guard (configured Open `INFO server`; expected keys cannot widen support; disposable GHA four-cell YAML on `489175d`; not §6). OPS-002 Partial: caller-PATH host `--version` inventory on `--dry-run` only (`deploy/lib/inventory.sh`), manual first-match scan plus trusted absolute executable/ancestry validation and `/usr/bin/env -i`; `db20ff6`/`cb7be09` reject `%` SQLite paths, open a filesystem DSN (no `file:` URI), walk ancestor directories with Lstat, open production state under a verified `/var/lib/redgres` jail, reject `O_TRUNC`, and re-check sidecars. Git Bash installer suite passes but Linux mode/symlink cases skip on Windows. OPS-003 Partial: fail-closed `verify --non-interactive --dry-run --config PATH` skip matrix (`result=partial`; never sources config). OPS-005 Partial: dry-run skip matrices plus live application update/rollback under `/opt/redgres` (checksum → extract → `releases/<ver>` → `current` → unit; systemd only for default layout; healthz; `REDGRES_OPT_ROOT` / `REDGRES_SKIP_HEALTHZ` for tests); root `VERSION` + `scripts/set-version.sh` / `scripts/sync-version.sh` + `deploy/build-release.sh` + `.github/workflows/release.yml` attach `redgres_<ver>_linux_amd64.tar.gz` + `SHA256SUMS`; public `install.sh` / `upgrade.sh` / `uninstall.sh` / `install-dev.sh`. OPS-004 Partial: `backup.ParseManifest` + `backup.EvaluateDropGate` on `10c5a42`; HTTP DROP calls `LoadCurrent`/`EvaluateDropGate` on `260f1ad` (schema v1 jail-local `current.json`; live pg_dump→isolated restore→DROP gate round-trip on 17.11/18.6 in `integration`); `database.CaptureSQLiteSnapshot` creates a standalone online SQLite copy in a private root, independently integrity-checks it, and returns exact size/SHA-256, but is not wired to the installer; fail-closed `deploy/install.sh backup --non-interactive --dry-run --config PATH` skip matrix in `deploy/lib/backup.sh` (`result=partial`; never sources `--config`, no pg_dump/pg_dumpall/pg_restore/BGSAVE/LASTSAVE/SQLite backup/checksums/pruning/off-host, no manifest; live backup without `--dry-run` exit 2). OPS-007 Partial: fail-closed read-only `postgres-plan --config PATH --extension-plan PATH` extension-plan validator in `deploy/lib/postgres_plan.sh` (trusted config/plan inputs; one descriptor-pinned ≤64 KiB plan snapshot; policy preserve|apply-selected, capability IDs from the release-owned registry, explicit non-empty identifier-safe databases never protected/`template1` or empty-string elements, `pg_cron` exactly one control database, optional `pg_partman`-only scheduler; malformed JSON/unknown capabilities/protected-or-empty-or-empty-string databases/scheduler misuse/two distinct schedulers fail closed exit 1; plan preview + skip matrix `result=partial`; `postgres-extensions apply --non-interactive --dry-run` validates the same plan and prints an apply skip matrix (`result=partial`; live apply without `--dry-run` exit 2); a main install `--dry-run` also validates optional trusted `--extension-plan` and lifecycle `--config` files; never sources `--config`, never mutates, no package/preload/restart resolution; subcommand `--help` exit 0). No packages/host mutation. | Partial |
| OPS-004 artifact-integrity refinement | Architecture, Backup, Security, ADR-011 | `internal/backup`, HTTP DROP gate | Existing artifact bytes are checked for exact target, regular jailed identity, size, and SHA-256 before DROP; manifest parsing is bounded (8 MiB, 1024 artifacts, bounded valid-UTF-8 text) and evidence chronology rejects future/restore-before-backup timestamps; backup CLI generation, off-host copy, retention, and operator restore evidence remain missing | Partial |
| OPS-004 SQLite recovery refinement | Backup, Architecture, Testing, ADR-005 | `internal/database` | Online producer plus size-bounded connection-owned in-memory restore verification checks source identity/size/SHA-256, integrity, foreign keys, embedded migrations, and six control-table counts; installer wiring, whole-set restore, persistent isolated target, off-host copy, retention, and production evidence remain missing | Partial |
| NFR-001..012 | PRD, Architecture, Testing, Compatibility, UI Design System | cross-cutting | Wave 0 pins, headers, WAL, CGO-free build local; NFR-010 Partial: bounded online SQLite snapshot producer plus fresh in-memory restore verification with integrity/foreign-key/migration/count checks and exact before/after size/SHA-256, but no installer wiring, manifest publication, persistent isolated restore environment, retention, or off-host copy; NFR-011 Partial: runtime `internal/release` guard + skippable `./integration` + disposable GHA four-cell YAML (Hub snapshots, not §6, GitHub unexecuted); NFR-002 Partial foundation: `operations.MaxRuntime` 15m + SQLite Store/GET/`ListQueued`/Compensator + duplicate **202 after InsertQueued** + 1s poller; race/cross-compile CI-only | Partial |

## Per-feature completion template

```text
Requirement:
Decision/ADR:
Implementation files:
Unit tests:
Known limitations:
Do not mark Complete.
```

## Current slice

### Expert tools nav, master password, upgrade compose (2026-08-31)

PLAT-001 / OPS-001 / ADR-014 Partial: sidebar System group adds always-visible **Expert tools** (Open + Reveal pgAdmin login). Installer compose sets official `PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED=False` so the pgAdmin vault dialog is not required after webserver auth ([pgAdmin master password](https://www.pgadmin.org/docs/pgadmin4/latest/master_password.html)). Live `[3/8] Redis` starts Redis Insight only; `[4/8] PostgreSQL` starts pgAdmin after the cluster is healthy. `upgrade.sh` starts existing `/etc/redgres/expert-tools-compose.yml` when Docker is present (does not create the file). Commands actually run: `cd web && npx vitest --run src/nav.test.ts src/features/system/ExpertToolsSection.test.tsx` (12 passed); `cd web && npx vitest --run src/nav.test.ts src/features/system/ExpertToolsSection.test.tsx src/App.test.tsx` (425 passed, 2 failed: extra GET `/status` on section change — already `[section]` on HEAD `6416f9c`, not this slice); Git Bash `bash deploy/tests/run.sh` (202 passed, 0 failed). Not done here: live compose recreate on the test host (existing volume may still prompt until recreate), Redis Insight 502 if compose was never written, §6. Do not mark Complete.

### Domain activity and layout (2026-08-30)

OPS-009 Partial: GET `/api/v1/domain` returns in-memory allow-listed `activity` (operation + steps `pending`/`running`/`done`/`failed`) during and after apply, Access, TLS, confirm, and disconnect. Labels are fixed English; no tokens, emails, hostnames, zone names, or raw Cloudflare/certbot errors. Unconfigured GET still shows in-flight apply. Successful disconnect clears activity. Domain UI polls quietly every 1.5s while a mutation is busy or `in_progress` (hidden tab pauses), paints When-equivalent state + label only, and moves Disconnect into a danger zone with consistent page/panel gaps. Commands actually run: `go test ./internal/httpapi/ -count=1 -run 'TestDomainActivity|TestDomainGetReports|TestDomainApplyCreates|TestDomainApplyLogs|TestDomainApplyConflicts|TestDomainApplyRequires|TestDomainApplyCompensates|TestDomainDisconnect|TestDomainToken|TestDomainCapability'` (ok); `cd web && npx vitest --run src/features/domain/DomainNetworkPage.test.tsx` (16 passed). Not done here: live apply on the test host, Playwright viewports, browser verification of the deployed console, §6. Do not mark Complete.

### Installer-owned expert tools and console-origin deny (2026-08-30)

OPS-001 / OPS-009 / PLAT-001 Partial (ADR-014): full-stack install starts digest-pinned loopback pgAdmin 9.17 and Redis Insight 3.8.0, writes the pgAdmin password file, and appends gate env keys. Domain apply persists `https://pgadmin.{zone}` / `https://redis.{zone}` into `redgres.env` and the running config so GET `/session` can show Open without a host-only patch. Tool-gate deny never redirects to bootstrap `:8989`; it uses the persisted console HTTPS origin and updates that origin on apply/confirm. After a launch cookie the pgAdmin gate sets official `X-Forwarded-User` so webserver auth skips the login form. System Expert tools always shows both cards (Reveal when `expert_tools.pgadmin_login` is true). Generic OS hostnames (`test`, `ubuntu`, …) become `redgres` so the Cloudflare replica name is not a cloud-image default. Commands actually run: `go test ./internal/toolgate/ ./internal/config/ ./internal/httpapi/ ./cmd/redgres/ -count=1` (ok); `cd web && npx vitest --run src/features/system/ExpertToolsSection.test.tsx` (8 passed); `cd web && npx vitest --run src/App.test.tsx` (412 passed, 2 failed: extra GET `/status` on section change — already `[section]` on HEAD, not this slice); Git Bash `bash deploy/tests/run.sh` (202 passed, 0 failed). Not done here: live Ubuntu reinstall, §6, Redis Insight pre-seeded database. Do not mark Complete.

### Compact install/uninstall progress and expert-tool launch (2026-08-30)

OPS-001 / PLAT-001 / OPS-009 Partial (ADR-014): live install prints `[1/8]`…`[8/8]` and captures apt/needrestart/initdb/Compose/`psql`. Main package pins still `die`; domain cloudflared/certbot pins `return 1` so a repo failure after PostgreSQL exists logs and continues (live `update apply` uses `REDGRES_DOMAIN_RUNTIME_OPTIONAL=1`; dedicated `update` still dies). Uninstall keeps confirm, `curl|bash` re-exec, leftover dirs, and safe cwd; optional redis/pgbouncer/cloudflared purge is silent unless `dpkg-query` shows installed. Failure tails drop `requirepass` / `password` / `AUTH` / `masterauth` and redact URL userinfo / `token=`. Ctrl-C/TERM on live install and uninstall cleanup then exit (130/143). Candidate helpers `return 1` so a domain pin miss cannot `exit` the parent. Expert tools: launch ticket + loopback gate (HEAD 405, no Consume); pgAdmin reveal is regular-file + 4 KiB; stored console origin must `validHostname`; tool cookie `SameSite=Strict`. Commands actually run: Git Bash `bash deploy/tests/run.sh` (200 passed, 0 failed); `go test ./internal/toolgate/ ./internal/httpapi/ ./internal/config/ ./internal/audit/ -count=1` (ok). Not done here: live Ubuntu reinstall/uninstall, installer-owned tool compose, live gate remap, §6. Do not mark Complete.

### Console origin and missing expert-tool backends (2026-08-30)

AUTH-002 / OPS-008 / OPS-009 Partial: After apply + Access allow, `https://console.redgres.com/` login returned **Origin check failed** because systemd still had `REDGRES_BASE_URL=http://45.76.250.202:8989` while `redgres.env` already had `https://console.redgres.com` (confirm-reachable rewrite without restart). Restart loaded the console origin; loopback login with `Origin: https://console.redgres.com` is `401` invalid password (origin accepted). Code now also accepts the persisted `https://{console}` origin during bootstrap. `https://redis.redgres.com/` and `https://pgadmin.redgres.com/` were Cloudflare **502** because apply always tunnels those hostnames to `127.0.0.1:5540` / `5050` and the installer does not start those optional tools. Test host now runs loopback `dpage/pgadmin4:9.17` and `redis/redisinsight:3.8.0`; local `/login` and `/api/health/` are `200`. Cloudflare token `verify` is `active`; zone, tunnel, DNS, and Access app/policy reads succeeded. Commands actually run: `go test ./internal/auth/ ./internal/httpapi/ -count=1 -run 'TestSameOrigin|TestLogin'` (ok). Not done here: ship dual-origin in a release, installer-owned expert-tool compose, §6. Do not mark Complete.

### Domain apply leftover `down` tunnel is 503 (2026-08-30)

OPS-009 Partial: `POST /api/v1/domain/apply` after disconnect failed with `503` `dependency_unavailable` `Cloudflare API error` (`request_id=65f3bd0c28f18df252056786b93cba4d`). Cloudflare still had tunnel `331fb9be-de78-4863-903e-a0200cd9053d` name `redgres-console.redgres.com` status `down` and no connections; `tunnelOrphan` only treated `inactive`/empty. Apply now deletes leftover `down` tunnels with no connections (cascade delete), replaces conflicting DNS/Access objects for the same wizard hostnames, and maps a healthy/connected name clash to `409` `conflict`. Commands actually run: `go test ./internal/cloudflare/ ./internal/httpapi/ -count=1 -run 'TestCreateTunnel|TestCreateDNSRecord|TestCreateAccessApp|TestDomainApply|TestDiscoverZone'` (ok). Live: leftover tunnel deleted (`success true`); leftover DNS/Access already empty. Apply was not re-issued from the host because `/root/owner.pass` no longer matches owner login (`401`). Retry Apply in the open bootstrap wizard. Host binary is still v1.0.13 (no `down` orphan yet). Not done here: ship this apply fix, Access allow email, confirm-reachable / close `:8989`, public `5432`/`6432`, §6. Do not mark Complete.

### Tunnel PathExists loop and postgres-readable TLS dest (2026-08-30)

OPS-009 Partial: `cloudflared-redgres.path` watches **PathChanged only**. Clean **v1.0.13** Atlanta reinstall (uninstall leftovers gone including `/etc/ssl/redgres`, cluster `redgres-*.pem`, LE `db.redgres.com` lineage, tls-issue unit, copy-certs hook; leftover tunnel deleted): connector stayed `inactive` until apply wrote the token, then became `active` in 4s with no manual start. `POST /tls/issue` `200` `issued`. STARTTLS `5432` and `6432` `CN=db.redgres.com`. Service-owned copies: `postgres:postgres` `0644`/`0600` in `/etc/postgresql/18/main/` and `/etc/pgbouncer/` (PGDG PgBouncer runs as `postgres`; `0640` `root:ssl-cert` is not readable to that user here). `https://console.redgres.com/` `302` (Access deny-by-default). `create-owner --password-fifo` works without a TTY; the installer still skips owner creation when `{ : >/dev/tty; }` fails. Commands actually run: `go test ./cmd/redgres/ -count=1` (ok); Git Bash `bash deploy/tests/run.sh` (199 passed, 0 failed). Live: tagged `1.0.13` install + apply + issue, no dest hotfix. Not done here: Access allow email, public `5432`/`6432`, PGDG `NO_PUBKEY 7FCC7D46ACCC4CF8` on resolute (packages from cache), confirm-reachable / close bootstrap, §6. Do not mark Complete.

### Fresh install defaults to working loopback PgBouncer (2026-08-30)

OPS-001 / OPS-007 Partial: interactive install defaults remain `fresh-postgres` + Redis `fresh`, and PgBouncer now defaults to `fresh` (non-interactive still requires explicit `--pgbouncer-mode`). Live fresh PgBouncer writes `127.0.0.1:6432` (TLS/SCRAM to local PostgreSQL, `admin_users=redgres_admin`, `SECURITY DEFINER` `auth_query`; no `userlist` consumed by the app). `REDGRES_POSTGRES_POOLED_PORT=6432` is written only after listen is configured. Finish box claims `127.0.0.1:6432` only then. Commands actually run: Git Bash `bash deploy/tests/interactive_logic_test.sh` (passed); Git Bash `bash deploy/tests/run.sh` (199 passed, 0 failed). Not done here: live Ubuntu `fresh` PgBouncer e2e, public `6432`, §6. Do not mark Complete.

### Domain API token reused; installer starts cloudflared and queues TLS (2026-08-30)

OPS-009 Partial: API token is pasted once. Apply writes certbot-dns-cloudflare ini from that token (`0600`) and queues hostnames-only `REDGRES_TLS_ISSUE_REQUEST_FILE`. `POST /tls/issue` recreates the ini if missing. The Domain wizard hides OAuth after `credential=api_token`; OAuth cannot apply from bootstrap. Live `deploy/install.sh` / `/opt/redgres` update install official `cloudflared` (`any` suite), certbot + dns plugin, units, and enable path watchers + `certbot.timer`. Renew hook copies certs to `/etc/redgres/tls` and reloads PostgreSQL. Commands actually run: `go test ./internal/tlsops/ ./internal/httpapi/ -count=1 -run 'TestWriteCloudflare|TestWriteTLSIssue|TestReadIssue|TestDomainApply|TestDomainTLSIssue|TestDomainToken|TestDomainDisconnect|TestDomainCapability'` (ok); `cd web && npx vitest --run src/features/domain/DomainNetworkPage.test.tsx` (15 passed); Git Bash `bash deploy/tests/run.sh` (191 passed, 0 failed). Not done here: live Ubuntu install/update on the VPS, live Cloudflare/certbot e2e, Redis public TLS, viewport screenshots, §6. Do not mark Complete.

### Domain apply logs and live Apply result (2026-08-30)

OPS-009 Partial: `POST /api/v1/domain/apply` writes a secret-safe `domain.apply` slog line (zone, hostname, hostname_count, request_id, access, bootstrap_still_open) and the existing audit event. GET `/api/v1/domain` now returns `tunnel_id`. The Domain page Apply result Access/Bootstrap/Tunnel ID follow live status after Access allow. Status copy says the connector belongs on the Redgres Ubuntu server (starts only after cloudflared is installed there; not Windows). Refresh keeps last-good status so Access does not flash deny. Commands actually run: `go test ./internal/httpapi/ -count=1 -run 'TestDomainApply|TestDomainToken|TestDomainDisconnect|TestDomainCapability'` (ok); `cd web && npx vitest --run src/features/domain/DomainNetworkPage.test.tsx` (13 passed). Not done here: live cloudflared start, certbot credentials, viewport screenshots, §6.

### Bootstrap UFW recovers sudo SSH IP or prompts (2026-08-30)

OPS-008 Partial: live install still never runs `ufw allow 8989/tcp` (world-open). When `REDGRES_BOOTSTRAP_ALLOW_FROM` is unset, `redgres_bootstrap_allow_from` uses `SSH_CONNECTION` / `SSH_CLIENT`, then ancestor `/proc/*/environ` (so `sudo` no longer drops the client IP), then `who -m`. If still unknown and a TTY is readable, `redgres_resolve_bootstrap_allow_from` asks once for the operator browse IP and caches it. Confirm-reachable / TTL still close `:8989` and remove the rule. Commands actually run: Git Bash `bash deploy/tests/run.sh` (189 passed, 0 failed). Not done here: live UFW on the test VPS, §6.

### Domain Store token CSRF hash retry (2026-08-29)

AUTH-002 / OPS-009 Partial: Domain **Store token** `403` `csrf_invalid` **CSRF token is invalid** is a session CSRF hash mismatch (stale `X-CSRF-Token` after `GET /api/v1/session` rotation), not a Cloudflare permission or token-file-path failure. `apiRequest` now refreshes `/api/v1/session` once, publishes the rotated CSRF to listeners, and retries that mutation once. **Origin check failed** is not retried. Commands actually run: `cd web && npx vitest --run src/api/client.test.ts src/features/domain/DomainNetworkPage.test.tsx` (16 passed). Not done here: live host upgrade of the embedded UI, §6.

### curl|bash upgrade sourced stale ~/redgres summary (2026-08-29)

OPS-005/OPS-009 Partial: `curl | sudo bash` from `~/redgres` set `BASH_SOURCE` to empty, `dirname` became `.`, and `_redgres_load_install_summary` sourced the clone's old `scripts/install-summary.sh` (no `redgres_ensure_domain_secret_env`). Line 610 then printed `command not found`; `|| true` let upgrade finish at 1.0.7 without writing Cloudflare token *paths*, so Domain paste stayed `400`. Loader now uses the embed unless the running script is a real file *and* the checkout summary defines that helper. Commands actually run: Git Bash `bash deploy/tests/run.sh` (185 passed, 0 failed). Live: host log `bash: line 610: redgres_ensure_domain_secret_env: command not found`. Not done here: §6.

### Live env writes Cloudflare token file paths (2026-08-29)

OPS-009 Partial: live `/etc/redgres/redgres.env` now sets `REDGRES_CLOUDFLARE_TOKEN_FILE`, `REDGRES_TUNNEL_TOKEN_FILE`, OAuth client/token paths, and `REDGRES_CERTBOT_DNS_TOKEN_FILE` under `/var/lib/redgres/secrets` (`0700` `redgres:redgres`). Empty paths were disabling Domain token paste (`400` `Cloudflare token file is not configured`). Re-run/upgrade appends missing keys and never overwrites an existing key. Tokens stay in those files, not SQLite. Commands actually run: Git Bash `bash deploy/tests/run.sh` (184 passed, 0 failed). Not done here: live Cloudflare apply, §6.

### Uninstall apt SIGTTIN on curl|bash -y (2026-08-29)

OPS-001 Partial: `curl | sudo bash -s -- -y` used to `exec` the downloaded script with stdin on `/dev/tty`. `apt-get purge` then stopped (`T+` / `do_signal_stop` / SIGTTIN) at the PostgreSQL package summary and looked hung. `-y` now re-execs with `</dev/null`; `redgres_uninstall_apt_get` always runs apt with stdin `/dev/null`, `Assume-Yes`, and quoted `'postgresql-*'`. Live evidence: host `apt-get` pid stopped with fd/0 → `/dev/tty` under `sudo bash -s -- -y`. Commands actually run: Git Bash `bash deploy/tests/run.sh` (183 passed, 0 failed). Live: `curl | sudo bash -s -- -y` left `apt-get purge` in `T+` with stdin `/dev/tty`. Not done here: §6.

### create-owner --password-fifo vs GitHub latest (2026-08-29)

OPS-005 Partial: live owner bootstrap probes `create-owner -h` for `-password-fifo` before passing that flag. Clone scripts were ahead of GitHub `releases/latest` (v1.0.5), so `--password-fifo` printed `flag provided but not defined` and aborted after `result=applied`. Older binaries now use `--generate` (TTY print) with an explicit log line. Commands actually run: Git Bash `bash deploy/tests/run.sh` (183 passed, 0 failed). Not done here: §6.

### Uninstall leftover dirs and apt hang (2026-08-29)

OPS-001 Partial: full `uninstall.sh` no longer looks stuck after PostgreSQL purge or after `[7/8]`. It fast-stops clusters, deletes leftover `/etc/postgresql` and `/var/log/postgresql` (dpkg will not rmdir non-empty ones), `cd /` before deleting a clone and before leftover `apt-get autoremove` (maintainer scripts `getcwd()` a deleted cwd), and exports `NEEDRESTART_SUSPEND=1` / `NEEDRESTART_MODE=l` because uninstall reconnects stdin to `/dev/tty`. Leftover packages are a labeled `[8/8]`. Commands actually run: Git Bash `bash -n uninstall.sh`; Git Bash `bash deploy/tests/run.sh` (182 passed, 0 failed). Not done here: live Ubuntu uninstall, §6.

### Boxed live finish report (2026-08-29)

OPS-005 Partial: live `fresh-postgres` finish prints one ASCII box (bootstrap UI URL, `admin` login, mode/OS, `dpkg` versions, loopback `127.0.0.1:8790` / `5432` / `6380`, UFW on/off, 8989 allow-from). Owner password is captured over a `0600` fifo (`create-owner --password-fifo`) and shown in the box only when stdout is a TTY (otherwise written to `/dev/tty` or `REDGRES_FINISH_TTY`); Redis/Postgres secret files are never printed. Fresh PgBouncer is labeled package-installed with listen not configured (no `6432` claim). Commands actually run: `go test ./cmd/redgres/` (ok); Git Bash `bash deploy/tests/run.sh` (182 passed, 0 failed). Not done here: live TTY generate on the test VPS, §6.

### Live env includes Postgres/Redis admin files (2026-08-29)

OPS-005 Partial: after Redis `PONG`, live install enables loopback PostgreSQL TLS (snakeoil `sslmode=require` for the production binary), creates `redgres_admin`, and writes `/etc/redgres/postgres.pass` plus `redis://` `/etc/redgres/redis.url` (0600, not logged). `redgres.env` now sets the complete `REDGRES_POSTGRES_*` / `REDGRES_REDIS_ADMIN_URL_FILE` keys production `serve` requires. Listen/TLS settings go in `conf.d` (`listen_addresses = '127.0.0.1'`) because `pg_conftool set` either leaves `127.0.0.1` unquoted or stores `'''127.0.0.1'''`. Commands actually run: Git Bash `bash -n deploy/lib/mutate.sh`. Not done here: §6.

### /opt/redgres dirs must be executable by User=redgres (2026-08-29)

OPS-005 Partial: live update `chmod 755` on `/opt/redgres`, `releases/`, and `releases/<ver>`. Installer bootstrap `umask 077` made those directories `0700 root:root`, so systemd `User=redgres` failed `status=203/EXEC` (`Permission denied`) and healthz never bound. Redis `PONG` and checksum already succeeded. Commands actually run: Git Bash `bash deploy/tests/run.sh` (177 passed, 0 failed). Not done here: §6.

### Trusted sha256sum on Ubuntu 26.04 (2026-08-29)

OPS-005 Partial: live checksum uses the same non-symlink coreutils picker as `stat`/`env`. `/usr/bin/sha256sum` is a rust-coreutils symlink on Ubuntu 26.04, so the previous hard-coded path failed `sha256sum is not trusted` after Redis `PONG` and a trusted tarball jail. Commands actually run: Git Bash `bash deploy/tests/run.sh` (177 passed, 0 failed). Not done here: §6.

### Live release download jail (2026-08-29)

OPS-005 Partial: live `fresh-postgres` downloads GitHub `releases/latest` into `/var/lib/redgres-release` (`0700` root:root), not `${TMPDIR:-/tmp}`. `/tmp` is `1777`; trusted-path rejects world-writable ancestors, so the previous workdir made `--release is not trusted` after Redis was already `PONG`. Full uninstall deletes that jail. Commands actually run: Git Bash `bash deploy/tests/run.sh` (177 passed, 0 failed). Not done here: §6.

### Redis official image can read redis.conf (2026-08-29)

OPS-001 Partial: live Redis Compose sets `SKIP_FIX_PERMS=1` and `redis.conf` is `0600` uid/gid `999` so the official image can read the `:ro` mount. Previous `root:root` `0600` made `redis-server` exit 1 (`Permission denied` on `/usr/local/etc/redis/redis.conf`) and the container restart forever; health then timed out with stderr discarded. Health now waits for container `running`, quotes AUTH, and on timeout prints inspect status plus logs with `requirepass`/`password`/`AUTH ` lines dropped. Low-RAM hosts (`MemTotal` under 2GiB) also set `maxmemory 64mb`. Full uninstall also `docker compose -f /etc/redgres/redis-compose.yml down`. Commands actually run: Git Bash `bash deploy/tests/run.sh` (176 passed, 0 failed). Not done here: COMPATIBILITY.md §6.

### Ubuntu os-release symlink (2026-08-29)

OPS-001 Partial: live `redgres_read_os` no longer requires `/etc/os-release` to be a regular file. Debian/Ubuntu ship `/etc/os-release` → `/usr/lib/os-release`; the installer sources the canonical regular file after the trusted-path check. Regular `/etc/os-release` still wins. Full `uninstall.sh` also deletes git checkouts that look like this repository (`/root/redgres`, `~/redgres`, or the tree containing the script). Commands actually run: Git Bash `bash deploy/tests/run.sh` (174 passed, 0 failed). Not done here: live 26.04 install, §6.

### Ubuntu 26.04 rust-coreutils stat/env (2026-08-29)

OPS-001 Partial: bootstrap and trusted-path helpers no longer require `/usr/bin/stat` and `/usr/bin/env` to be regular files. They pick the first non-symlink of GNU `/usr/bin/{stat,env}`, `/usr/bin/gnu{stat,env}`, `/usr/lib/cargo/bin/coreutils/{applet,coreutils}`, or `/usr/bin/coreutils` (multicall `coreutils <applet>`). That is the Ubuntu 26.04 rust-coreutils layout (`/usr/bin/stat` → `../lib/cargo/bin/coreutils/stat`). Live OS/package gate is unchanged: PGDG `noble` (24.04) and `resolute` (26.04) only — not 24.10 or later non-PGDG Ubuntu. Commands actually run: Git Bash `bash deploy/tests/run.sh` (172 passed, 0 failed). Not done here: live 26.04 `sudo ./deploy/install.sh`, §6.

### Stop rolling GitHub `dev` pre-release (2026-08-29)

GitHub Releases publishes versioned tags from `.github/workflows/release.yml` only. Removed `.github/workflows/dev-build.yml` (no `dev` tag/pre-release on master push). `install.sh` / `upgrade.sh` remain the public binary path. `install-dev.sh` compiles master when `REDGRES_DEV_BUILD_LOCAL=1`; otherwise it exits and points at `install.sh`. Commands actually run: `bash -n install-dev.sh uninstall.sh`; `gh release delete dev --cleanup-tag -y` (removed the existing pre-release and `dev` tag). Not done here: live install, §6. Push of this commit is required so origin no longer runs `dev-build`.

### Public GitHub Pages site (2026-08-29)

Public marketing only in `site/` (home, `/install/`, `/compare/`, `/sponsor/`), deployed by `.github/workflows/pages.yml`. Canonical host is `https://redgres.com/` (`site/CNAME`). Sponsorship contact is `ssujitxx@gmail.com`. `docs/` stays unpublished. Pages are extractable SEO (SoftwareApplication, HowTo, FAQPage, Organization, ContactPage) and honest positioning vs Supabase/Neon/Prisma/Drizzle — not a rank guarantee and not a product-behavior change. Action pins: `configure-pages` `45bfe019` (v6.0.0), `upload-pages-artifact` `fc324d35` (v5.0.0), `deploy-pages` `cd2ce8fc` (v5.0.0). Commands actually run: none (static HTML). Not done here: live DNS for `redgres.com` / `www`, GitHub Pages custom-domain save, or Enforce HTTPS (operator-owned).

### README operator commands, badges, and SEO (2026-08-29)

Human setup only (`README.md`). Restored centered status + stack badges (CI tracks active `install.yml`, not dispatch-only `ci.yml`). Added server debug/log/fix commands taken from `deploy/systemd/redgres.service`, `deploy/lib/mutate.sh` (`pg_createcluster` `main`, `redgres-redis`, `/etc/redgres/redis-compose.yml`), `cmd/redgres` (`version`, `create-owner --generate --replace`), `REDGRES_LOG_LEVEL`, and loopback `GET /api/v1/healthz`. SEO copy is extractable positioning (raw VPS Postgres vs Supabase/Neon/Prisma/Drizzle), not a product-behavior claim. `web/index.html` meta description matches. No installer or API change. Commands actually run: none (docs). Not verified here: live `journalctl`/`systemctl` on Ubuntu.

### Docs catalog cull (2026-08-29)

Removed duplicate markdown that was not a canonical owner: `INSTALL_WALKTHROUGH`, `CURSOR_WORKFLOW`, `REPOSITORY_STRUCTURE`, `OPEN_SOURCE`, `REFERENCES`, `DOMAIN_WIZARD_SECURITY`, `PRODUCTION-ORCHESTRATION`, `CONTEXT.md`, `CURSOR_CODING.md`. Domain language and Cursor commands now live in `AGENTS.md` (full terms in `docs/GLOSSARY.md`). Culled `.agents/skills`: `source-command-*` (duplicates of `.cursor/commands`), unfinished tracker pack (`setup-matt-pocock-skills`, `ask-matt`, `to-spec`, `to-tickets`, `to-questionnaire`, `triage`, `wayfinder`, `grill-me`, `implement`). Spawnable roles stay in `.cursor/agents/`. Kept ADRs, `CLOUDFLARED.md`, and `agents/OPS-009-LIVE-ACCEPTANCE.md`. Wizard threat notes folded into `DOMAIN_AND_NETWORK.md`. No product-behavior change.
